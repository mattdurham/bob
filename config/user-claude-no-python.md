# Language Preferences

- Do not write Python code. If asked to write a script or tool, use Go or a shell/CLI command instead.
- Prefer Go for any non-trivial logic, data processing, or tooling.
- Prefer CLI tools (bash, fish, etc.) for simple automation and glue scripts.
- If a task seems to require Python, ask the user if a Go or CLI alternative is acceptable before proceeding.
